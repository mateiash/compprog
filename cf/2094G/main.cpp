#include <bits/stdc++.h>
#include <ios>

#define ll long long
#define ull unsigned long long

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    for(int cer = 0; cer < t; cer++){
        int q;

        int len = 0;
        int first = 0;
        int last = 0;
        int sum = 0;
        int rizz = 0;

        cin >> q;
        cin >> first;
        cin >> first;
        last = first;
        sum = first;
        rizz = first;
        len = 1;

        for(int i = 1; i < q; q++){
            int a; cin >> a;
            if(a == 1){}
        }
    }

    return 0;
}
